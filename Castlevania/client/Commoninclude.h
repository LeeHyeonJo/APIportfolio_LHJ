// 이 파일의 역할:: 라이브러리에 구현된 자료구조들을 가져와서 사용할거임. 

#pragma once 
#include <vector> // 배열 라이브러리 
#include <list> // 링크드 리스트 라이브러리 
#include <map> // 트리 구조 
#include <set> // 해시 테이블 구조 
#include <functional> // 함수 포인터도 객체로 저장해서 쉽게 사용할 수 있음. 

// firework 헤더의 역할 (원래 있는 자료형의 이름을 편한대로 바꿔준 것) 
// 이것을 삭제하고 여기로 옮긴다?? <- 질문하기 
#include "firework.h"
#include "math" // 수학 기능. 설정이 안되는 이유 질문하기 


